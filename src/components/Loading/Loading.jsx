import React from "react";
import { LoaderWrapper, LoaderIcon } from "./Loading.styles";

function Loading() {
  return (
    <LoaderWrapper>
      <LoaderIcon />
    </LoaderWrapper>
  );
}

export default Loading;
